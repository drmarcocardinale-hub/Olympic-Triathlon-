# Core
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # parquet
pyyaml>=6.0

# Scraping (Phase 0)
requests>=2.31
beautifulsoup4>=4.12
lxml>=5.0

# Weather + WBGT (Phase 3)
openmeteo-requests>=1.2   # Open-Meteo historical API client
requests-cache>=1.1
retry-requests>=2.0
pythermalcomfort>=2.10    # WBGT + PHS / thermoregulation models

# Modelling (Phases 2, 4, 5)
statsmodels>=0.14         # MixedLM (random intercepts/slopes)
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44

# Optional, recommended for richer random-slope models
# pymer4>=0.8             # wraps R lme4 (needs R installed)
# bambi>=0.13            # Bayesian multilevel models on PyMC

# Plotting
matplotlib>=3.8
seaborn>=0.13
